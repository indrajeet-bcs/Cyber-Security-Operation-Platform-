# Backend API URL for deployed frontend (Render)
# When running locally via `npm run dev`, the Vite proxy handles /api → localhost:8000
# Uncomment and set this for local dev if you want to hit the deployed backend instead:
# VITE_API_BASE_URL=https://cyber-security-backend-3o19.onrender.com/api
